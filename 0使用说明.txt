================================================================================
                              VitePress 文档使用说明
================================================================================

📖 官方文档参考
    地址：https://vitepress.dev/zh/guide/getting-started

================================================================================
🚀 本地开发环境启动
================================================================================

操作步骤：
    1. 打开命令行工具（cmd 或 PowerShell）
    2. 切换到 doc 目录
    3. 执行以下命令：

    npm run docs:dev

重要提示：
    • config.mts 文件用于修改目录结构配置
    • 启动后可在浏览器中预览文档效果

================================================================================
📦 生产环境构建
================================================================================

构建命令：
    npm run docs:build

构建输出：
    生产的 HTML 文件将保存在以下目录：
    C:\work\www\doc.dmxapi.cn\.vitepress\dist

说明：
    • 此命令会生成静态 HTML 文件
    • 生成的文件可直接部署到 Web 服务器

================================================================================
🖼️ 图片水印工具
================================================================================

工具文件：
    add_watermark.py

功能说明：
    • 为图片添加水印效果
    • 支持批量处理图片文件
    • 直接运行即可使用

使用方法：
    python add_watermark.py

================================================================================
📝 VitePress 代码分组语法示例
================================================================================

使用场景：
    • 同时展示多种实现方式（如不同语言/库）
    • 用户可以点击标签切换查看

语法格式：

::: code-group

```python [Requests]
import requests

response = requests.post(
    "https://www.dmxapi.cn/v1/chat/completions",
    headers={"Authorization": "Bearer YOUR_API_KEY"},
    json={"model": "gpt-4", "messages": [{"role": "user", "content": "Hello"}]}
)
print(response.json())
```

```python [OpenAI SDK]
from openai import OpenAI

client = OpenAI(
    api_key="YOUR_API_KEY",
    base_url="https://www.dmxapi.cn/v1"
)

response = client.chat.completions.create(
    model="gpt-4",
    messages=[{"role": "user", "content": "Hello"}]
)
print(response.choices[0].message.content)
```
:::

注意事项：
    • 每个代码块的 [标签名] 会显示为可点击的选项卡
    • 必须以 ::: code-group 开始，以 ::: 结束
    • 代码块之间不需要空行

================================================================================